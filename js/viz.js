'use strict';

var app = angular.module('VizApp', []);

app.controller('VizController', ['$scope', '$rootScope', '$http', '$compile', function ($scope, $rootScope, $http, $compile) {

    $scope.init = function () {

    };

}]);
